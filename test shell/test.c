#include <stdbool.h>

bool is_digit(char c){
    //if (c >= 48 && c <= 57){
    if (c >= '0' && c <= '9'){
        return true;
    } else {
        return false;
    }
    // return (c >= );
}

bool is_upper(char c){
    return is_upper() || is_lower();
}   

bool is_print(char c){
    
}
    
int main(void){
    return 0;
}